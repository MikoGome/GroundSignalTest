
const initialState = {
  isOpen: false,
  data: {}
};

function moduleReducer(state=initialState, action) {
  switch(action.type) {
    
    default:
      return state;
  }
}

export default moduleReducer;